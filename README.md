# VO_converter

A basic video and audio converter.

## Getting started

This repository is now initialized with a minimal Python CLI project.
The output format is inferred by ffmpeg from the output file extension.

```bash
python vo_converter.py --help
```
