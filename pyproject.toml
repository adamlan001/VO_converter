[project]
name = "vo-converter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ffmpeg-python>=0.2.0",
    "pyqt6>=6.11.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.0",
]
